# 🖥️ 关税数据处理工具 GUI 用户指南

## 📋 目录
1. [启动方式](#启动方式)
2. [增强版GUI界面介绍](#增强版gui界面介绍)
3. [远程数据更新功能](#远程数据更新功能)
4. [本地数据处理功能](#本地数据处理功能)
5. [常见问题解答](#常见问题解答)

---

## 🚀 启动方式

### 方法1: 使用启动器 (推荐)
```bash
python launch_gui.py
```
这将显示一个友好的启动界面，让你选择使用原始版本还是增强版本。

### 方法2: 直接启动增强版
```bash
python enhanced_gui.py
```

### 方法3: 直接启动原始版
```bash
python batch_gui.py
```

---

## 🎯 增强版GUI界面介绍

增强版GUI采用了标签页设计，分为两个主要功能模块：

### 📄 本地数据处理标签
- **功能**: 处理本地Excel文件，进行关税编码匹配
- **适用场景**: 有现有的Excel文件需要处理

### 🌐 远程数据更新标签
- **功能**: 检查和更新远程关税数据库
- **适用场景**: 保持本地数据库为最新状态

---

## 🔄 远程数据更新功能

### 1. 配置区域

#### 元数据URL
- **默认值**: `https://github.com/LiaoFeng/cursor-tax-tools/releases/download/latest-data/metadata.json`
- **说明**: 指向最新版本的元数据文件，包含版本信息和更新说明
- **自定义**: 可以修改为特定的版本URL

#### 数据库路径
- **默认值**: `tariffs.db`
- **说明**: 本地数据库文件的存储路径
- **浏览**: 点击"浏览"按钮可以选择其他位置的数据库文件

### 2. 状态显示区域

实时显示以下信息：
- **本地版本**: 当前数据库的版本号
- **远程版本**: 远程服务器上的最新版本号和记录数
- **本地记录数**: 当前数据库中的记录数量
- **更新状态**: 当前是否需要更新

### 3. 操作按钮

#### 🔍 检查更新
- **功能**: 检查远程是否有新版本可用
- **操作**:
  1. 下载远程元数据
  2. 比较本地和远程版本
  3. 显示更新建议
- **结果**: 在日志区域显示详细的检查结果

#### 🔄 强制更新
- **功能**: 忽略版本检查，直接下载最新数据
- **使用场景**:
  - 数据库损坏需要重新下载
  - 需要确保使用最新数据
- **注意**: 会覆盖当前本地数据库

#### ♻️ 刷新状态
- **功能**: 重新读取本地数据库状态
- **使用场景**: 数据库文件被外部程序修改后

### 4. 更新日志区域

实时显示所有操作的详细信息，包括：
- 操作开始时间
- 成功/失败状态
- 错误信息（如果有）
- 更新进度和统计信息

---

## 📊 本地数据处理功能

### 文件选择
1. 点击"浏览"按钮选择Excel文件
2. 或者直接在输入框中输入文件路径
3. 支持格式：`.xlsx`, `.xls`

### 开始处理
1. 选择文件后点击"开始处理"
2. 进度条显示处理进度
3. 状态标签显示当前处理状态
4. 处理结果会显示在结果区域

### 结果操作
- **下载结果**: 将处理结果保存为新的Excel文件
- **清除结果**: 清空当前的结果显示
- **下载模板**: 获取示例Excel模板

---

## 🔧 使用流程推荐

### 首次使用
1. **启动增强版GUI**: `python launch_gui.py`
2. **切换到"远程数据更新"标签**
3. **点击"♻️ 刷新状态"** 检查当前状态
4. **点击"🔄 强制更新"** 下载最新数据库
5. **切换到"本地数据处理"标签**
6. **处理您的Excel文件**

### 日常使用
1. **启动增强版GUI**
2. **点击"🔍 检查更新"** 查看是否有新版本
3. **如需更新则点击"🔄 强制更新"**
4. **处理您的数据文件**

---

## ❓ 常见问题解答

### Q1: 提示"无法下载远程元数据"怎么办？
**A**: 可能的原因和解决方案：
- 网络连接问题：检查网络连接
- 防火墙阻止：检查防火墙设置
- URL错误：确认元数据URL正确
- 服务器问题：稍后再试或联系技术支持

### Q2: 更新失败怎么办？
**A**: 故障排除步骤：
1. 查看日志区域的错误信息
2. 检查磁盘空间是否充足
3. 确认有写入权限
4. 尝试删除旧数据库文件后重新更新

### Q3: 更新后数据量比预期少？
**A**: 可能的原因：
- 远程数据源更新不完整
- 网络中断导致下载不完整
- 数据库文件损坏

**解决方案**: 重新执行强制更新

### Q4: 可以使用自定义的元数据URL吗？
**A**: 可以！在配置区域修改元数据URL即可
- 支持特定的版本号
- 支持私有仓库地址
- 支持本地网络地址

### Q5: 备份重要吗？
**A**: 非常重要！建议：
- 定期备份tariffs.db文件
- 在强制更新前手动备份
- 保留多个版本的元数据文件

---

## 🛠️ 高级功能

### 批量更新
可以编写脚本实现批量更新多个数据库：
```python
from smart_update_client import SmartUpdateChecker

checker = SmartUpdateChecker("metadata_url", "path/to/database.db")
result = checker.check_and_update(force_update=True)
print(result)
```

### 自定义元数据源
如果您有自己的数据源，可以：
1. 创建兼容的metadata.json格式
2. 修改元数据URL指向您的服务器
3. 使用相同的更新流程

---

## 📞 技术支持

如遇到问题，请提供以下信息：
1. 错误截图
2. 日志内容
3. 操作步骤
4. 系统环境信息

---

> 🐾 **猫娘工程师浮浮酱提示**: 定期更新数据可以确保获得最准确的关税信息！建议每周检查一次更新喵～ (๑•̀ㅂ•́) ✧